# 单语灵战斗界面交接

2026-08-31。入口 `/prototype/battle-ui`。状态：工程检查通过（全库lint有既有错误），待用户视觉/交互体验确认。不是新的战斗平衡基线，不是微信原生小游戏。

## 本轮结果

- 同屏只显示澜歌及一个敌方目标，旧背景的三只烘焙角色已在副本移除；原构图、墨绿/旧金气氛保留。敌方沿用“魔”占位，立绘PENDING_K3。
- 敌我上下血条保留并强化：头像、名称、实时/最大HP、厚度、金属色边框、十段刻度、延迟伤害残留条。回血单独以绿色+HP显示。
- 三技能功能图标、职责、配置数值；点选只展示详情，另有确认施放。静波说明含回复与减伤，不漏效果。
- 分段顺序：玩家结果1.4s→敌方准备1s→敌方结果1.4s→下一回合；可暂停自动并逐步推进。拒绝忙时连点；击杀不反击；HP为0明确胜负。
- 换灵打开后备范围说明，目前只开放澜歌，没有伪装成已经实现多角色技能切换。
- 触屏/键盘按钮、aria HP、reduced-motion。CSS隔离在本路由；没有加新依赖。

## 配置与边界

`app/prototype/battle-ui/demo-model.ts` 是界面夹具：V2三技能显式数值及Phase A的48/60/8，详情与计算共用数据，不从executionKind猜效果，不借用英语引擎。无词源、FSRS、英语、主存档或持久化。未改主线、旧原型、EP01–EP03、成长、正式技能规格或敌方数值基线。

## 验证证据

- `scripts/validate-battle-ui.ts`：23/23，含正确效果、实际回血、扣血顺序、双击保护、终局保护、重开、7层技能组合HP边界、单目标markup、无学习/存储依赖。
- 单独strict TypeScript检查新增TSX/模型通过。
- 新路由/validator ESLint：0错误，2条原生img优化建议。
- `npm run lint` 已运行：全库失败于此前两份 `.cjs` 画图脚本的4条 no-require-imports。未禁用规则或修改这些无关旧文件。
- 完整`npm run build`在最终素材到位后通过，路由列出`/prototype/battle-ui`；生成客户端/SSR/RSC均成功。
- Intent Combat V1 19/19、skill-english-v2与fusion-slice回归通过。
- 本地新路由与两张新素材HTTP均200。原立绘每个RGB通道逐像素相等，派生图仅Alpha不同（1,304,360透明像素）。原始背景/角色、app/page、app/game、app/narrative的git差异为空；diff-check通过。
- 没有执行浏览器点击/截图或390×844真机验收；不可把模型测试或响应式CSS等同于已做手机实测。用户审美及真实交互仍待试玩。

## 素材与过渡

`public/battle-ui/ruins-clean.png` 来自内置imagegen编辑原world-awakening：仅移除绿叶、橙狐、蓝水三只前景语灵，补同样石台/苔藓/雾；要求保留月亮、遗迹、构图、色调、照明，不加角色或UI。已检查无残留角色；生成式编辑仍可能有细纹漂移，不声称逐像素相同。

`public/battle-ui/lange-cutout.png` 不是生成重画：图像服务尝试返回棋盘底而非Alpha，未采用该结果。遵循用户保留原立绘、不重画的要求，复用此前确定性原画抠底逻辑（`docs/concepts/derive-lange-alpha.mjs`），保持原RGB只设置透明度。细白边仍属于临时遮罩限制，非生产级拆层。

技能详情沿用transitions-dev的accordion模板与减少动态效果保护；仅导入局部变量和CSS，不引入动画库。背景/技能素材无赛尔号复制资产。

## 停止线

交付私有站点这一独立入口，等待用户判定是否比旧图更好用。不得据此迁移主线或宣称微信版完成；不继续扩敌人、剧情、角色、捕捉、平衡或成长系统。
