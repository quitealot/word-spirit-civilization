# 烬尾动作连贯性小修

用户反馈：方向/站姿还行，有点卡顿。授权继续优化；不改造型、数值和技能机制。

代码观察：旧去程通过left逐帧参与布局，跑步两帧约192ms切换，四个SVG帧各有drop-shadow。不能凭此断言用户设备真实掉帧，未做浏览器性能采样。

本轮：仅tail-melee.css改为translate3d位移（演员40%/44%宽度折算，桌面终点仍40%场宽，移动端仍38%）；will-change只在焰尾施放时存在；移除每帧重复阴影；跑步从两次步态循环变三次，每128ms切换。保留硬切步态避免双腿重影，不宣称生成新中间姿势。

1280ms命中、2752ms回位、3200ms结束不变；扫尾/回跳方向、素材、原站姿、HP/冷却、其他技能及主线不变。减少动态规则仍关闭动作。新增连续覆盖与移动端坐标换算检查。仍是两帧步态，扫尾/回跳姿势切换的局限未消失。

状态：工程验证后发布；动态连贯性和真实帧率待用户试玩，不能宣称已解决掉帧。

工程结果：焰尾17/17、烬尾21/21通过，build/diff通过。新增断言检查连续换帧无空档/重叠、桌面和手机接触坐标等价。全仓lint仍既有4条CJS require错误13条图片警告。本轮仅CSS运行改动，未修改图片或战斗结算。
